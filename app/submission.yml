setup:
  - command: "pip install -r requirements.txt"
  - command: "docker-compose build"

test:
  - command: "docker-compose run --rm app pytest"

benchmark:
  - command: "docker-compose run --rm app python scripts/run_benchmark.py"